# -*- coding: utf-8 -*-
"""
Config template — copy to config.py and fill in your values.
Do NOT commit config.py — it contains secrets.
"""
import os


# MongoDB connection — supports auth via URI
# Format: mongodb://user:password@host:port/dbname
# Or set MONGODB_HOST, MONGODB_PORT, MONGODB_DB individually (no auth)
MONGODB_URI = os.environ.get('MONGODB_URI', '')

DEFAULT_TITLE = "MetaMuseum"

BASE_DIR = os.getcwd()

SQLALCHEMY_TRACK_MODIFICATIONS = False
SECRET_KEY = os.environ.get('SECRET_KEY', 'change-me-in_production')
SECURITY_PASSWORD_SALT = os.environ.get('SECURITY_PASSWORD_SALT', 'change-me-in-production')

FLASK_ADMIN_SWATCH = 'cerulean'


MAIL_SERVER = os.environ.get('MAIL_SERVER', 'smtp.gmail.com')
MAIL_PORT = int(os.environ.get('MAIL_PORT', 465))
MAIL_USE_SSL = os.environ.get('MAIL_USE_SSL', 'True') == 'True'
MAIL_USERNAME = os.environ.get('MAIL_USERNAME', '')  # Gmail address
MAIL_PASSWORD = os.environ.get('MAIL_PASSWORD', '')  # App password
MAIL_DEFAULT_SENDER = os.environ.get('MAIL_DEFAULT_SENDER', MAIL_USERNAME)

# LLM keys are set via Flask-Admin → LLMConfig model (stored in MongoDB)
# Whisper keys are set via Flask-Admin → WhisperConfig model
# MINIMAX_API_KEY is no longer needed here — configure in admin panel


AVATAR_OPTIONS = {
    'shiba': {'name': 'Shiba Dog', 'model': None},
    'robot': {'name': 'Robot', 'model': None},
    'none': {'name': 'No Avatar', 'model': None},
}

print("BASE_DIR: ", BASE_DIR)
